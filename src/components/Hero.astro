<section class="hero">
  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">B2B Marketing · AI · Growth Systems</p>
      <h1>Building better B2B marketing engines.</h1>
      <p class="lede">I’m a B2B marketing leader exploring how AI, better systems, and stronger operating models can make marketing teams more effective.</p>
      <p>I write about what I’m learning, experiment with new approaches, and occasionally build things to test the ideas.</p>
      <div class="actions">
        <a class="btn btn-primary" href="/ideas">Explore ideas <span>→</span></a>
        <a class="btn btn-secondary" href="https://www.linkedin.com/in/heidivandermeer" target="_blank" rel="noreferrer">Connect on LinkedIn ↗</a>
      </div>
    </div>
   <div class="hero-visual" aria-hidden="true">
    <img
      src="/hero-growth-systems.png"
      alt=""
      width="1536"
      height="864"
      />
    </div>
  </div>
</section>
<style>
.hero { padding:92px 0 112px; overflow:hidden; }
.hero-grid { display:grid; grid-template-columns:minmax(0,1.08fr) minmax(380px,.92fr); gap:72px; align-items:center; }
.hero-copy { max-width:760px; }
.hero-copy h1 { margin-bottom:28px; max-width:760px; }
.lede { font-size:clamp(1.15rem,2vw,1.35rem); color:var(--deep-ink); max-width:690px; }
.hero-copy p:not(.eyebrow) { max-width:680px; }
.actions { display:flex; gap:14px; flex-wrap:wrap; margin-top:30px; }
.hero-visual { position: relative; min-height: 480px;  display: grid;  place-items: center; }
.hero-visual img { display: block; width: min(100%, 680px); height: auto; object-fit: contain; }
@media (max-width: 980px) { .hero-grid { grid-template-columns:1fr; gap:28px; } .hero-visual { min-height:auto; max-width:620px; } }
@media (max-width: 600px) { .hero { padding:68px 0 78px; } .hero-visual { margin-top:6px; } }
</style>
