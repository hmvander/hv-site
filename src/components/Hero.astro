<section class="hero">
  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">B2B Marketing · AI in GTM · Growth Systems</p>
      <h1>Building better B2B marketing engines.</h1>
      <p class="lede">I’m a B2B marketing leader exploring how growth systems, AI, and GTM models can make marketing teams more effective at driving revenue.</p>
      <div class="actions">
        <a class="btn btn-primary" href="/ideas">Explore ideas <span>→</span></a>
        <a class="btn btn-secondary" href="https://www.linkedin.com/in/heidivandermeer" target="_blank" rel="noreferrer">Connect on LinkedIn ↗</a>
      </div>
    </div>
   <div class="hero-visual" aria-hidden="true">
    <img
      src="/hero-growth-systems.png"
      alt=""
      width="1536"
      height="864"
      />
    </div>
  </div>
</section>
<style>
.hero { padding: 60px 0 30px; overflow:hidden; }
.hero-grid { display:grid; grid-template-columns:minmax(0,1.08fr) minmax(440px,.92fr); gap:32px; align-items:start; }
.hero-copy { max-width:680px; }
.hero-copy .eyebrow { margin-bottom:28px; }
.hero-copy h1 { max-width:660px; margin-bottom:26px; font-size:clamp(4.6rem,6.6vw,5.75rem); line-height:1; }
.lede { margin-bottom:22px; font-size:clamp(1.15rem,2vw,1.35rem); color:var(--deep-ink); max-width:650px; }
.hero-copy p:not(.eyebrow) { max-width:640px; }
.actions { display:flex; gap:14px; flex-wrap:wrap; margin-top:24px; }
.hero-visual { position:relative; min-height:420px; display:grid; justify-items:start; align-items:start; margin-top:-44px; }
.hero-visual img { display:block; width:min(112%,680px); max-width:none; height:auto; object-fit:contain; }
@media (max-width:1100px) {
  .hero-grid { grid-template-columns:minmax(0,1fr) minmax(400px,1fr); }
  .hero-visual img { width:100%; max-width:680px; }
}
@media (max-width:980px) {
  .hero { padding-top:64px; }
  .hero-grid { grid-template-columns:1fr; gap:36px; }
  .hero-copy h1 { font-size:clamp(3.6rem,9vw,5rem); }
  .hero-visual { min-height:auto; width:min(100%,680px); max-width:none; margin-top:0; justify-items:center; }
}
@media (max-width:600px) {
  .hero { padding:56px 0 72px; }
  .hero-copy .eyebrow { margin-bottom:22px; }
  .hero-copy h1 { margin-bottom:24px; font-size:clamp(2.7rem,13vw,4.2rem); }
  .hero-visual { margin-top:4px; }
}
</style>
