<section class="connect-section" id="connect">
  <div class="container connect-inner">
    <p class="eyebrow">Connect</p>
    <h2>Let’s connect.</h2>
    <p>I’m always interested in meeting people thinking about B2B growth, marketing leadership, AI, or how modern marketing teams should operate.</p>
    <div class="actions">
      <a class="btn btn-primary" href="https://www.linkedin.com/in/heidivandermeer" target="_blank" rel="noreferrer">Connect on LinkedIn ↗</a>
    </div>
  </div>
</section>
<style>
.connect-section { background:var(--deep-ink); color:var(--shell); padding:104px 0 86px; }
.connect-inner { max-width:880px; margin-inline:auto; }
.connect-section .eyebrow { color:var(--sea-glass); }
.connect-section h2 { color:var(--white); margin-bottom:24px; }
.connect-section p:not(.eyebrow) { font-size:1.18rem; max-width:760px; color:color-mix(in srgb,var(--shell) 82%, transparent); }
.actions { display:flex; flex-wrap:wrap; gap:18px; align-items:center; margin-top:30px; }
.email-link { color:var(--shell); font-weight:800; }
</style>
